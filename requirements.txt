# BEKE — production dependencies
# Generated from .venv on 2026-05-31 (Python 3.14 local / 3.11 container)
# Install: pip install -r requirements.txt
# For CPU-only torch (container): see Dockerfile — installs from PyTorch CPU index

# ---------------------------------------------------------------------------
# LangChain stack
# ---------------------------------------------------------------------------
langchain==1.3.1
langchain-classic==1.0.7
langchain-community==0.4.2
langchain-core==1.4.0
langchain-chroma==1.1.0
langchain-experimental==0.4.2
langchain-huggingface==1.2.2
langchain-openai==1.2.2
langchain-postgres==0.0.17
langchain-text-splitters==1.1.2

# ---------------------------------------------------------------------------
# Vector store + database
# ---------------------------------------------------------------------------
pgvector==0.3.6
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
psycopg2-binary==2.9.12
chromadb==1.5.9

# ---------------------------------------------------------------------------
# Embeddings + reranking
# torch pulled in as a dependency of sentence-transformers
# ---------------------------------------------------------------------------
sentence-transformers==5.5.1
transformers==5.9.0
huggingface_hub==1.16.1
torch==2.12.0

# ---------------------------------------------------------------------------
# LLM + OpenAI
# ---------------------------------------------------------------------------
openai==2.38.0

# ---------------------------------------------------------------------------
# AWS
# ---------------------------------------------------------------------------
boto3==1.43.14

# ---------------------------------------------------------------------------
# Document parsing (needed by src/ingestion/loaders.py imports)
# ---------------------------------------------------------------------------
pypdf==6.12.2
python-docx==1.2.0
python-pptx==1.0.2

# ---------------------------------------------------------------------------
# Web interface
# ---------------------------------------------------------------------------
streamlit==1.57.0

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
python-dotenv==1.2.2
bcrypt==4.2.1
PyYAML==6.0.2
